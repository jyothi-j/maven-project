pipeline {
    agent any
	stages{
	    stage('Init'){
		    steps {
			    echo "Testing by Jyothi .."
			}
		}
		
		stage('Build'){
		    steps {
			    echo "Building .."
			}
		}
		
		stage('Deploy'){
		    steps {
			    echo "Code Deployed.."
			}
		}
	}
}
